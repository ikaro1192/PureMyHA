[Unit]
Description=PureMyHA MySQL HA Daemon
After=network.target

[Service]
Type=simple
ExecStart=/usr/sbin/purermyhad --config /etc/purermyha/config.yaml
ExecReload=/bin/kill -HUP $MAINPID
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
